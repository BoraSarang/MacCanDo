# 2026-08-17 T-09 세션 로그

## 완료: 시리즈 허브(A) + 상세(B) 개편 + 데이터 5개
1. Series에 imageUrl(커버)/intro(취지, MD) 필드 추가 — Neon drift로 migrate dev 불가 → 수동 ALTER + migrate resolve로 적용 (8개 마이그레이션 up to date)
2. 웹 API: 시리즈 생성/수정(PATCH)에 imageUrl/intro 허용, admin 목록에도 포함
3. /series (A): 카드형 허브 — 커버(없으면 그라디언트)+제목+설명+N개의 글, 편 목록 제거
4. /series/[id] (B): 커버 풀 + 취지(markdown) + 1~N편 목록 + generateMetadata (template에 통합해 중복 제거)
5. macOS: 시리즈 생성/수정을 시트(sheet)로 전환, 커버 URL + 취지(TextEditor) 추가, 상세 헤더에 커버/취지 표시 — 빌드 성공
6. 데이터: 신규 시리즈 3개 (맥 파일 관리/맥 보안·프라이버시/맥과 아이폰 연동) + 신규 글 6편 (전부 발행) → 총 시리즈 5개·글 13편
7. 커버: public/series/*.svg 3개 (그라디언트 배너) — macOS 업로드로 교체 가능
8. E2E: 허브 카드 5개/카드→상세/커버+취지 검증 추가 — 9/9 통과
9. zsh 주의: VAR=x cmd 형태에서 인자 확장 안 됨 (export 필요) — curl 401 삽질 원인

## 이슈 참고
- updatePost seriesId 보호 (MacCanDo-59p) — 이전 세션 수정 완료
- seriesOrder 잔재 버그 (MacCanDo-cu9) — macOS MarkdownRenderer 표 미지원 포함

## 후속: 커버 이미지 실전 적용
- "맥과 아이폰 연동" 시리즈 커버를 실제 이미지로 교체 (macOS 배경화면 Mac Blue → /uploads/series-iphone-cover.jpg, 1600px JPEG 220KB)
- 발견: PATCH /api/admin/series/[id]가 body.description ?? null 로 부분 수정 시 description/intro/imageUrl을 null 리셋 — ?? null 제거로 미전달 필드 유지 수정
- 허브/상세에서 커버·설명·취지 확인 완료 (a11y 스냅샷 — 텍스트 전용 모델)

## T-10 세션 2: 역할 카테고리(다대다) + 태그 도입 (v2.2)

1. 사용자 확정: 카테고리 6개 역할 기반(Develop/Design/Work/Productivity/System/Media) + 카테고리 다대다 + 태그 자유 생성
2. 스키마: PostCategory/Tag/PostTag 조인 + Post.contentType(ARTICLE/TIP/NEWS) + Category.description
3. Neon 수동 마이그레이션: 20260817010000(조인+contentType) / 20000(description) / 30000(contentType enum) — db execute + migrate resolve
4. 주의: 첫 db execute에서 INSERT 순서 꼬임으로 기존 categoryId 데이터 소실 — 새 카테고리로 재매핑으로 해결 (어차피 교체 예정)
5. 데이터: 카테고리 6개 + 글 13편 매핑(17건) + 태그 19개(22건) + NEWS 글 1편(macOS Tahoe) 추가
6. lib/posts.ts: getPosts 카테고리/태그/contentType 필터(string_agg), getPostBySlug 다대다 include, resolveCategoryIds(slug/id 허용)/resolveTagIds(자유 생성)
7. 웹: 메뉴 개편(맥 앱|맥 팁|맥 소식|시리즈), /apps 허브(좌측 필터), /tips /news /tag/[slug], 글 상세 태그, PostCard 다중 배지, sitemap 확장
8. macOS: EditorView 카테고리 토글 다중 선택 + 태그 필드 + 글 타입 Picker, PostsView 카테고리/태그 표시 — BUILD SUCCEEDED
9. E2E 15/15 통과 (허브 필터 6개/Develop 2개/다중 배지/팁 7개/뉴스 1개/태그 모아보기)
10. 커밋 3건 푸시 완료 (e115b8b, 829543b, 96c1331)

## T-11 세션: 홈 개편(광고 슬롯) + 세부 페이지 확장 + 정렬 (v2.3)

1. 사용자 확정: 홈 = 시리즈 배너(광고 슬롯) → 추천 → 최근 → 역할 탐색
2. "광고" 관리자 메뉴 신설 (시리즈 배너 + 추천 글 지정, macOS 탭 + 웹 admin 탭)
3. 지정 없거나 모자라면 자동 채움 (시리즈: 전체 최신순 / 추천: 조회수 top)
4. 세부 페이지: 관련 게시글(태그 공유 3개→카테고리 폴백) + 이전/다음글(일반 글만)
5. /apps 정렬 드롭다운 (최신순/조회수순) — 팁/뉴스/카테고리/태그에도 통일
6. 스키마: Post.featuredOrder + Series.featuredOrder (Neon 수동 마이그레이션)

## T-11 세션 2: 홈 개편(광고 슬롯) 완료 (v2.3)

1. 스키마: Post.featuredOrder + Series.featuredOrder — Neon 수동 마이그레이션 20260817040000
2. 홈: 시리즈 배너(광고 슬롯, SeriesBanner) → 추천 게시글(FeaturedPosts 대형1+소형2) → 최근 6 → 역할 탐색
3. 자동 채움: 배너=전체 시리즈(featuredOrder asc), 추천=조회수 top
4. 관리자 "광고" 탭: 웹 admin(AdsManager ★ 토글) + macOS(AdsView) — PATCH posts/[id]/featured 신설
5. 정렬 드롭다운(최신순/조회수순): /apps /tips /news /category/[slug] /tag/[slug]
6. 글 상세: 관련 게시글(태그 공유→카테고리 폴백 3개) + 이전/다음(비시리즈만, 동일 발행일 id 폴백 — 데모 데이터가 같은 시각 발행이라 필수였음)
7. 버그: 한글 태그 slug가 디코딩 안 되어 /tag/애플 404 → decodeSlug 추가
8. 시드 보정: dev-mac-productivity-apps/iterm2-tmux-oh-my-zsh/Tahoe 매핑 완료 (PostCategory 21, PostTag 30, Tag 23)
9. tsx 실행 시 --env-file=.env 필요 (dotenv 미로드 ECONNREFUSED)
10. E2E 21/21, TSC OK, macOS BUILD SUCCEEDED, 커밋 3건 푸시 완료 (f76329e, e62329e, 8fad255)
11. 데모 지정 상태: 추천 Homebrew(1)+Tahoe(2), 배너 맥 파일 관리(1)
