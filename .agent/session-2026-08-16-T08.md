# 세션 로그 — T-08 (2026-08-16)

1. 무엇을: T-08 macos 승인/통계/업로드/백업/AI SEO/동기화 완료
2. 플랫폼: macos + web
3. 결과:
   - 댓글 승인: CommentsView 완성, 웹 GET /api/admin/comments?status= 필터 추가
   - 통계: StatsView (카드 6종 + Swift Charts 2개), totalViews 필드명 수정
   - AI SEO: GeminiService (REST, gemini-2.0-flash, JSON 파싱), 설정 키 저장, 에디터 sparkles 버튼 + 적용 시트
   - 백업/복원: 웹 GET/POST /api/admin/backup (lib/backup.ts, LWW), macOS 설정 UI, slug 누락 버그 수정
   - 동기화: 웹 POST /api/admin/sync/bulk (lib/sync.ts), macOS 초안 DB slug 컬럼 마이그레이션, 설정 버튼
   - E-WEB-VALID-1002 추가
4. 검증: curl 백업(카테고리4/글3/댓글3) → 복원(글2+skip1 LWW) → sync/bulk(1건 생성→삭제), TS 에러 0, macOS BUILD SUCCEEDED
5. 남은 TODO: T-10 사용자 UI 확인 후 close (2칸 에디터/이미지 업로드/AI SEO), T-06 잔여, Vercel 배포 시 이미지 R2 전환
6. 문서: TODO.md, CHANGELOG.md 갱신, PLAN_v1.1.0_macos.md
7. 주의: dev 서버 Neon 지연 (백업 P95 1759ms — 배포 시 R2/캐시), Gemini 키 미설정 상태 (설정 필요)
8. bd: T-08(MacCanDo-39e) close 대기, T-10(MacCanDo-T10) open

--- T-08 보강 (16:00~16:15): 이미지 목록 DB화
- 사용자 요구: 이미지 파일 목록(업로드 시각/사용처/캡션)을 DB로 관리, 파일↔DB sync
- Image 모델 + 수동 마이그레이션 (Neon은 shadow DB 미지원 → migrate dev 불가, --create-only도 실패 → migration.sql 수동 작성 + migrate deploy 성공)
- uploads API 개편 (POST 캡션/GET sync/PATCH 캡션/DELETE 파일+DB), 사용처 추적 (createPost/updatePost → trackImageUsage, 글 삭제 SetNull)
- macOS: 캡션 표시/편집(연필)/사용처 표시, 삽입 시 DB 캡션 자동
- dev 서버 재시작 필요했음 (Prisma 클라이언트 갱신) — 16:05 재시작
- 주의: E-WEB-VALID-1002 메시지 일반화, name 파라미터는 "YYYYMMDD/파일.ext" 형식

--- T-08 보강 2 (16:30~16:45): AI SEO → 웹 meta 태그 자동 구성
- 사용자 기대: AI SEO 결과가 웹 페이지 meta 태그로 자동 구성될 줄 알았다 → 실제 연결 완료
- seoMeta 필드를 실제로 사용 (기존엔 잔재): macOS 에디터 저장 시 전송, 웹 generateMetadata가 우선 사용
- 대시보드/앱 목록에 SEO 표시 (✨ + 툴팁)
- Gemini 모델 gemini-2.0-flash 폐기됨(404) → gemini-3.7-flash로 교체 (키 목록에서 최신 flash)
- 검증: title/description/keywords/og:title/og:description 반영 확인, 테스트 글 삭제
- 남은 것: 사용자 확인 (에디터에서 AI SEO 적용 → 저장 → 웹 페이지 보기)

--- T-08 보강 3 (17:00~17:10): AI SEO 시트 개편
- 저장된 seoMeta 있으면 AI 호출 없이 저장값 표시 + 재생성 버튼 (사용자 요구)
- 시트에 og:image(대표 이미지) 추가: 본문 첫 [img:] 자동 추출, 수동 편집, Gemini에 후보 전달
- 메타 태그 미리보기 (title/description/keywords/og:image HTML 형태)
- SeoMeta.image 필드 추가, 웹 og:image = seoMeta.image ?? thumbnailUrl
- 검증: og:image 반영 확인, 빌드+tsc 통과

--- T-08 보강 4 (17:20~17:35): AI SEO 캐시 + close
- seo_cache SQLite LRU (100건, SHA256 키 = 제목+본문+슬러그+이미지)
- 재생성/다시 생성 = forceRefresh (캐시 우회), 첫 클릭 = 캐시 hit 시 즉시 반환
- 설정에 캐시 히트율 표시, [CACHE] 로그
- DraftStore v3: seo_meta 컬럼 (초안에 SEO 저장 — 서버 저장 전에도 유지)
- 검증: 빌드 성공, 사용자 확인 후 bd close

--- AI 도우미 (17:40~18:00)
- 사용자 요청: 게시글 작성 시 AI 도우미 버튼 → 별도 창 → 프로그램 이름/웹사이트 URL 입력 → 소개/비교/장점/특이사항/추천 이유 MD 생성 → 참고용으로 복사해서 사용
- AssistantView.swift 신규 (xcodegen), GeminiService.generateProductGuide + fetchURLText + stripHTML + callTextWithRetry
- 비교 대상 지정/자동, 캐시(guide: prefix) + 재조회, 원문/미리보기 토글 + 복사 버튼
- 검증: CleanMyMac X 생성 OK (503 일시 오류 → 재시도), 로컬 URL fetch OK, 빌드 성공
- 에러코드: E-MAC-AI-1004, E-MAC-VALID-1003 (error_message_ko.json + web 복사)

--- 시리즈 기능 (18:05~18:40)
- 사용자 요구: ① 글을 1편/2편/3편 시리즈로 묶기 ② 게시글 하단 시리즈 목록 + 현재 강조 + 클릭 이동 ③ 시리즈 관리 화면 (시리즈 아닌 글 선택 → 시리즈로, 드래그 순서) ④ 에디터에서도 시리즈 등록 ⑤ 웹 /series + /series/[id] 모아보기 ⑥ 일반 목록에도 order 순 출력, 라벨 없음 ⑦ 웹 admin 시리즈 탭 ⑧ 초안 숨김 ⑨ 이미지 없음 (첫 편 썸네일로 대체)
- 구현: Series 테이블 + Post.seriesId/Order (migration 20260816110000), 시리즈 API 5종, macOS SeriesView(드래그 onMove→즉시 저장), 에디터 드롭다운(+새 시리즈), /series + /series/[id] + 게시글 하단 SeriesList, 홈/카테고리/검색 raw SQL 정렬(시리즈 그룹 = 최신 편 발행일, order asc), admin 시리즈 탭(↑↓)
- 트러블: raw SQL $N 파라미터 바인딩 오류 → Prisma.sql 조각 + Prisma.join으로 수정
- 검증: 시리즈 생성→글 3개 추가→순서[3,1,2]→웹 반영, 2편 '보고 있는 글' 배지, 1편(DRAFT) 숨김, 홈 Homebrew→TetherLens order 순, 검색 정상, macOS 빌드 성공
- 남은: macOS 앱에서 시리즈 화면/드래그 수동 확인, 테스트 시리즈('Test 시리즈 검증') 정리 여부

--- 시리즈 화면 개선 (17:45~17:50)
- 사용자: 오른쪽 시리즈 글 목록 세로 너무 큼 → 최대 5편 높이, 하단 시리즈 없는 글 목록 확대, 검색 추가
- 서버: getPostsWithoutSeries(q) — title ILIKE, updatedAt desc (최근 글부터), GET /api/admin/series?q=
- macOS: SeriesView 검색 TextField + 300ms 디바운스 서버 검색, 시리즈 글 목록 frame(height: min(count,5)*34+8), 하단 목록 flex 전체
- 트러블: TetherLens가 시리즈에서 빠져 있던 데이터 정합성 → 재추가 (2편 Homebrew, 3편 TetherLens)
- 검증: q=clean 서버 매칭 OK, 빌드 성공, 앱 재실행
